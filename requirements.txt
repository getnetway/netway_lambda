# Core Lambda dependencies — always bundled in the zip.
# boto3/botocore are intentionally excluded: the Lambda Python 3.11 runtime
# already provides them, bundling duplicates adds ~30 MB unzipped for nothing.
httpx==0.27.0
requests>=2.31.0
python-dotenv==1.2.2
